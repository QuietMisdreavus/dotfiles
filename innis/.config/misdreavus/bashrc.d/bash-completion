# i can't/don't want to modify the system bashrc on macos, so this sources the bash-completion
# sources installed by homebrew
[[ -r "/usr/local/etc/profile.d/bash_completion.sh" ]] && source "/usr/local/etc/profile.d/bash_completion.sh"
